source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '8.0'

install! 'cocoapods', :deterministic_uuids => false

target 'gRRCTest' do
  # Depend on the generated gRRCTest library.
    pod 'gRRCTest', :path => '.'
end
